# Prompt templates

## Python CLI

Create a complete Python CLI project from scratch.

Goal:
Build a CLI app that does X.

Constraints:
- Python 3.12
- Use uv if available, otherwise venv + pip
- Use pyproject.toml
- Use src/ layout
- Use typer
- Use pytest
- Use ruff
- Add README.md
- Keep the first version minimal but working
- Run tests and fix failures

Before editing files, propose the file structure.

## FastAPI

Create a complete FastAPI project from scratch.

Goal:
Build a small API that does X.

Constraints:
- Python 3.12
- Use uv if available, otherwise venv + pip
- Use pyproject.toml
- Use src/ layout
- Use FastAPI and uvicorn
- Use pytest and httpx
- Add /health
- Add one real feature endpoint
- Add README.md with curl examples
- Run tests and fix failures

Before editing files, propose the file structure.

## Django

Create a minimal Django project from scratch.

Goal:
Build a Django app that does X.

Constraints:
- Python 3.12
- Use uv if available, otherwise venv + pip
- Use SQLite initially
- Use class-based views only if useful
- Add tests
- Add README.md
- Keep the first version small
- Run tests and fix failures

Before editing files, propose the file structure.
